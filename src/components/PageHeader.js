import React from "react";

export const PageHeader = ({ title }) => (
  <h2 className="has-text-weight-bold is-size-1 is-size-2-mobile is-medium is-primary page-header-tile has-text-centered">
    {title}
  </h2>
);
